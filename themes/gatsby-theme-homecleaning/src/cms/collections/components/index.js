import cta from "./cta";
import footer from "./footer";
import navBar from "./navBar";
import stickyBar from "./stickyBar";

const components = {
  label: "Components",
  name: "components",
  delete: false,
  files: [cta, stickyBar, navBar, footer]
};

export default components;
